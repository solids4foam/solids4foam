/*--------------------------------*- C++ -*----------------------------------*\
| solids4foam: solid mechanics and fluid-solid interaction simulations        |
| Version:     v2.3                                                           |
| Web:         https://solids4foam.github.io                                  |
| Disclaimer:  This offering is not approved or endorsed by OpenCFD Limited,  |
|              producer and distributor of the OpenFOAM software via          |
|              www.openfoam.com, and owner of the OPENFOAM® and OpenCFD®      |
|              trade marks.                                                   |
\*---------------------------------------------------------------------------*/
FoamFile
{
    version     2.0;
    format      ascii;
    class       volVectorField;
    object      U;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

dimensions      [0 1 -1 0 0 0 0];

internalField   uniform (0 0 0);

boundaryField
{
    flag
    {
        type            fixedValue;
        value           uniform (0 0 0);
    }
    outlet
    {
        type            zeroGradient;
    }
    inlet
    {
        type            fixedValue;
        value           uniform (1.0 0 0);

        // type            codedFixedValue;
        // name            smoothRampedFixedValue;
        // code
        // #{
        //     // Velocity magnitude after transition period
        //     scalar UMag = 1.0;

        //     // Smoothly ramp the velocity magnitude over the rampPeriod
        //     const scalar t = this->db().time().value();
        //     const scalar rampPeriod = 2;
        //     if (t < rampPeriod)
        //     {
        //         UMag *= (1.0 - cos(M_PI*t/rampPeriod))/2.0;
        //     }

        //     // Add a small Uy component to excite the vortex shedding
        //     const scalar t1 = 3.0;
        //     const scalar t2 = 4.0;
        //     if (t > t1 && t < t2)
        //     {
        //         // Apply UMag in a direction slightly offset from x
        //         operator==(UMag*vector(1, 0.1*sin(M_PI*(t - t1)/(t2 - t1)), 0));
        //     }
        //     else
        //     {
        //         // Apply UMag in the x direction
        //         operator==(UMag*vector(1, 0, 0));
        //     }
        // #};

        // value             uniform (0 0 0);
    }
    walls
    {
        type            slip;
    }
    block
    {
        type            fixedValue;
        value           uniform (0 0 0);
    }
}

// ************************************************************************* //
